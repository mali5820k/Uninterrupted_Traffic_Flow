### PYTHON 3 API
##### Python Flask server (basic files setup but we're not using it)
##### Currently working on a websocket server using the Pithikos python-websocket-server
##### OpenCV will be running and sending data to the server for the app to poll data from.